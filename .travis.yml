os: linux

language: python
python:
  - "2.7"

install:
  - sudo apt-get install muscle
  - pip install --upgrade pip
  - pip install --upgrade setuptools
  - make setup

script:
  - make test

branches:
  only:
    - master
    - /^issue-[0-9]+.*$/
